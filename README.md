# Mesh Directory

A lightweight user directory UI built with Next.js (App Router) + TypeScript.

## What it does
- Displays a directory of users fetched from a public API
- Supports search + sorting
- Includes a user detail page

## Tech
- Next.js (App Router)
- TypeScript

## Running locally
```bash
npm install
npm run dev
